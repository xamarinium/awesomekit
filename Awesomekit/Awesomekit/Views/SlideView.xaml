<Grid x:Class="Awesomekit.Views.SlideView"
      xmlns="http://xamarin.com/schemas/2014/forms"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ContentView Grid.Row="0" Padding="60,30,60,0">
        <Image x:Name="ImageSlide" Source="{Binding Image}" Aspect="AspectFit" />
    </ContentView>

    <ContentView Grid.Row="1" Padding="20,10,20,50">
        <Label Text="{Binding Description}"
               Style="{StaticResource LightLabel}"
               HorizontalTextAlignment="Center" />
    </ContentView>
</Grid>